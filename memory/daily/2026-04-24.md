# 2026-04-24

## Summary
- **Personal Tasks — major refactor day.** Shipped four iterations on the skill in one morning:
  1. **Future-deadline filter.** Default `list` now hides tasks with `due_date > today + 1 month` (Volvo winter tires, etc.). `--all` shows everything.
  2. **Grouping discussion.** Tried by-urgency (rejected by Vovan), then by-action-type (also rejected), settled on **grouping by location/tag**.
  3. **Tag schema, multi-value.** Started as `location TEXT` enum-style, then upgraded to `tags TEXT[]` so a task can belong to multiple tags. CHECK constraint pinned to `['дача','квартира']` only — adding new tag = single ALTER plus update `normalizeTag()`/`TAG_HEADERS`/`TAG_ORDER_SQL`. Groups render as 🌲 Дача → 🏠 Квартира → 📍 Прочее.
  4. **Timezone bug fix.** Дача facade task was on the wrong day in the cron output. Root cause: writing/reading dates through `new Date().toISOString()` shifts by TZ offset. Fix: read with `TO_CHAR(due_date, 'YYYY-MM-DD')`, write with `$N::date` cast — Postgres handles dates as strings, JS Date never touches them. Migrated 10 existing rows `-1 day`.
- **USER.md updated.** Family: Жена — Ксюшенька; Сын — **Владик (2021 г.р.)**; Дочка — **Лерочка (2015 г.р.)**. Added Владик to Russian-language list. Big correction: садик выбираем для Владика, не Лерочки.
- **Kindergarten decision flipped back to №587** (Скрипникова 58а). Yesterday landed on 149 because Богдана is there, today reversed — final pick is 587 (newest, ~5-7 min walk per the new map). Built second annotated Yandex map relative to Лобанка 22 with ranking 587 > 149 > 471 > 17.
- **CRM productive day.** Three 1:1s auto-summarized (Alexander/Vlad, Vlad/Evgeny, Roman/Vlad), plus Nuevo System Sync. Total ~18 pending action items by EOD.
- **KB ingest.** Hormozi "Business Books Tier List" YouTube short (1 chunk, $0.002).
- **YouTube Shorts brief.** Filled the "Покажу как" template for Ostap — садик-research workflow as the example case. Iterated 3× per Vovan/Ксюшенька feedback (Лерочка→Владик age fix, Богдана-isn't-the-изюминка, final pick 587).
- **Side asks.** Discord/Sublime icon read; Vichy 48H vs 72H deodorant recommendation for Ксюшенька; Diadal rose + Андрейченко currant gardening advice (zone 5a-5b, укрытие обязательно).

## Corrections
- **Kindergarten is for Владик**, not Лерочка. Mental model fix: Владик = 4 yo (2021), Лерочка = 10 yo (2015), already в школе.
- **Don't group tasks by urgency or action-type** — Vovan rejected both. Group by **tags** (location-style, but renameable, multi-value).
- **Tags are `TEXT[]` array, not single column.** A task can be both дача and квартира.
- **Don't use `new Date().toISOString()` round-trip on Postgres `date` columns** — TZ shift causes off-by-one bugs. Always `TO_CHAR` on read and `::date` cast on write.
- **Final садик pick reverted to 587** — изюминка про Богдану/149 убрать из шорта-брифа.

## Skills used
- **Personal Tasks** — heavy refactor (tags array, CHECK constraint, group rendering, +1mo hide filter, TZ bug fix, migration). Multiple cron dry-runs to verify output. Closed tasks ПВХ-плитку, Shoplazza. Added #37 (карточка Лерочке).
- **CRM** — 3 1:1 syncs + Nuevo System Sync + morning/evening Slack recaps. 18 pending items accumulated.
- **Knowledge Base** — Hormozi YouTube Short ingest (item `e72ffd62-...`).
- **agent-browser / google-search / Yandex Maps** — садик map v2 (sharp+SVG overlay), photo collage from Yandex.Картинки, Vichy product comparison.
- **memory** — none directly today (USER.md edits don't go through memory-save).
- **Book Summary / Slack / Render Image** — not used.
