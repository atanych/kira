# 2026-06-19

## Summary
- **Personal-tasks: новый тег `volatclaw`.** Vovan голосом попросил завести тег (Whisper покрошил в "wallet-clone" — переспросила, оказалось `volatclaw`). Добавила `volatclaw` в CHECK constraint `tasks` + `task_templates`, обновила `normalizeTag`/`TAG_HEADERS`/`TAG_ORDER_SQL`/`primaryGroup` + `migrate.js` + SKILL.md. Закинула 3 задачи в тег: Chrome file delivery flapping, прокси после security-апдейта, voice transcription скачет между языками.
- **DB ownership: 11 таблиц переехали под `bot_kira`.** Сначала упёрлась в "нет прав на ALTER" — Vovan подсветил, что я **сама должна** мочь шевелить свои таблицы. Передала ownership всех 11 таблиц схемы `kira` с `postgres` → `bot_kira`. Теперь делаю DDL без postgres-роли.
- **Server path migration (`/home/claude` → `/home/volatclaw`).** Vovan дал инструкцию заменить хардкод `/home/claude/volatclaw/...` на относительные пути или `$BOT_DIR`, и заменить `/home/claude/volatclaw/bin/proxied` на просто `proxied` (теперь на PATH). Поправила: `skills/wb/wb-search.ts`, `crons/real-estate-digest.json` (выпилила 3 хардкод-команды → инструкция к google-search global skill), `skills/book-summary/SKILL.md` (историческая заметка про LD_LIBRARY_PATH). Снесла `skills/market-scan/__pycache__/`. Закоммитила `fe3453d`. Verified: `grep /home/claude` пусто, `proxied by echo ok` → ok, `proxied by curl realt.by` → HTTP 200.
- **Home Base: Vovan спрашивал про куст — вейгела (Weigela florida).** Дала список питомников и рынков в Минске + сезон посадки.
- **Лерочка спрашивала, что купить в самолёт.** Список (антистресс/раскраски/книжка-квест), скорректировала после уточнения, что подушка-комфортки у неё уже есть.

## Corrections
- Vovan: "Ты разве не можешь менять сама свои таблички?" → проверила права (owner=postgres, я под `bot_kira`), сделала ownership transfer на все 11 таблиц. Корректный фикс — owner-transfer одноразово, а не "просить postgres каждый раз".
- Я попыталась "wallet-clone" принять как имя тега — переспросила вместо того чтобы наугад создать тег. Это правильное поведение, оставила в саму себя.

## Skills used
- `personal-tasks` — добавила тег `volatclaw`, перенесла задачи, обновила код/SKILL.md.
- `my-db` (через CRM_DATABASE_URL) — ALTER TABLE … OWNER TO bot_kira × 11 + DROP/ADD CONSTRAINT.
- `cron` — почистила `real-estate-digest.json` от хардкод-путей.
- `memory` — сохранила ownership-факт.
