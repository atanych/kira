# 2026-07-12

## Summary
- **Vovan хочет закопаться в embeddings + BM25** (задача #78 "изучить текущие embeddings и BM25"). Расписала пейзаж: **dense** (sentence-transformers, OpenAI text-embedding-3, Voyage, Cohere, bge, nomic) для семантики, **sparse** (SPLADE, BM25-как-вектор) для точных терминов, **multi-vector / late interaction** (ColBERT, ColPali) — хранилище ×100 но точнее. **Гибрид dense+sparse почти всегда бьёт чистый dense на -5-15% recall.** Модальности: текст, код (CodeBERT/Voyage-code-3), мультимодал (CLIP/SigLIP/ImageBind), домен (BiomedBERT/LegalBERT). Архитектурные тренды 2025-26: **Matryoshka** (bge-m3, OpenAI-3 — можно truncate 3072→512 без потерь), **instruction-tuned** (e5-instruct, instructor, Voyage-3 — префикс "represent this for search:" даёт boost), **late chunking** (2024, эмбеддишь весь док потом чанкишь).
- **BM25 разбор.** 1994, основа Elasticsearch/Lucene/Postgres FTS. TF с saturation (не спамишь ключами), IDF (редкие слова весят больше), length normalization (короткий док с совпадением важнее). Отличие от эмбеддингов: BM25 = лексический (точное совпадение), embeddings = семантический (синонимы/смысл). Гибрид BM25+dense через **RRF (Reciprocal Rank Fusion)** — стандарт RAG 2023-24. Postgres умеет из коробки: `tsvector` + `ts_rank_cd` близко к BM25.
- **YouTube learning path для векторов/embeddings/BM25.** Собрала кураторскую подборку: 3Blue1Brown (визуал), StatQuest (Josh Starmer, для 5-летнего), **James Briggs / Aurelio Labs** (топ канал по vector search — плейлист "Vector Search for AI"), Underfitted (Santiago, короткие hybrid search), Anthropic Contextual Retrieval блог (+67% recall — must read), LlamaIndex channel (Jerry Liu про advanced RAG).
- **Vovan — ревью loose entries в MEMORY.md.** Собрала HTML-триаж: 5 KEEP / 10 MERGE / 2 CAN DELETE / 5 MUST DELETE. Vovan одобрил план. Спросил почему self-review не делает это еженедельно. Разобрала SKILL.md self-review — **есть дыры:** (1) `memory-merge-existing` (loose → в **существующий** topic/project, не только новый) отсутствует, (2) `memory-obsolete` (описан done-work без будущей ценности) отсутствует, (3) `memory-supersede` (новая запись покрывает старую полностью, но не текстовый дубль — dedup не ловит) отсутствует, (4) `memory-capacity` есть в категориях но **триггер не задокументирован** — распухший MEMORY.md никто не флажит. Self-review потом (вечером того же дня) отработал и сделал большой cleanup: 4 memory-graduation (ksusha/vovan/lerochka/vladik топики → projects/people/), memory-capacity 25555 → 10689 байт (58% reduction), несколько prune-superseded/obsolete.
- **Vovan попросил задачу на вторник** — сделать МА (мобильное приложение?) для BYD Song+ и поставить приложения. #121, вт 14 июля. Уточнила МА = мобильное приложение — ответа не было.

## Corrections
- Нет прямых корректировок. Vovan сам обнаружил через self-review triage что скилл не покрывает merge-into-existing / obsolete / supersede — это моё slip: skill.md надо было улучшить раньше.

## Skills used
- `personal-tasks` — #78 (изучить embeddings/BM25) активна, +#121 (МА для Song+ на вторник).
- `self-review` — ran автоматически 21:12 UTC, применил 4 graduation + capacity + несколько prune. Full report в tmp/logs/self-review-2026-07-12.md.
- `memory` — для сейва.
